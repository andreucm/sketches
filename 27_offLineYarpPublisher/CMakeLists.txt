
CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
FIND_PACKAGE(YARP)
INCLUDE_DIRECTORIES(${YARP_INCLUDE_DIRS})

ADD_EXECUTABLE(olp_test offLinePublisher.cpp main_test.cpp)
TARGET_LINK_LIBRARIES(olp_test ${YARP_LIBRARIES})

